.navbar.navbar-fixed-top
  .navbar-inner
    .container
      -# TODO need to define app_name url
      =link_to image_tag('zen_logo.png', id:'zen-logo') + ' ' + app_name, www_url, {class:'brand'}
      %ul.nav
        -# TODO need to define home final url
        %li{:class => active('stats#index')}
          =link_to 'Statistics', stats_path
      %ul.nav.pull-right
        %li
          =menu_link_icon_to 'signout', signout_path
.navbar-push
